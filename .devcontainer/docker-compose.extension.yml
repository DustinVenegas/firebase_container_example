services:
  devcontainer:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    command: sleep infinity
    volumes:
      - .:/workspaces:cached
    networks:
      - example_net

networks:
  example_net:
    external: false
